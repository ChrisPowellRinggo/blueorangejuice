# Claude Instructions

## Session Start
At the start of every session, read all markdown files in the project root to get context on the current state of the game before doing anything else. Currently: GAME_PLAN.md.

## Project
Untitled multiplayer first-person shooter game.
- Server: Node.js + Express + Socket.io (`server/`)
- Client: browser-based 3D (`client/`)
- Shared constants and utils (`shared/`)
